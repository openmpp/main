* If `CanDie` is `false`, the values in [`ProbMort`](#ProbMort) have no effect.
* Setting `CanDie` to `false` can make it easier to understand
 the interaction of other events,
 such as Fertility and Union Formation.