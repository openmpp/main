openMpp: Read and write OpenM++ database from R

You can update input parameters or select results OpenM++ model runs.

Following operations supported:
  - get model definition (parameters and output tables definition);
  - create or modify working set of input parameters to run OpenM++ models;
  - read model output tables expression values and accumulators
  - read model input parameters values

Examples can be found in R/openMpp/inst subdirectory 
and you can use models/bin/*.sqlite database files to run it.

More information about OpenM++ at http://ompp.sourceforge.net/wiki/
